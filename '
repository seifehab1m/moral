import { strapi } from ".";

export async function upload(file: File) {
  const res = await strapi({
    path: "/upload",
    init: {
      method: "POST",
      body: file,
    },
  });

  console.log(res);

  return res;
}
